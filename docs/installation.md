# Installation

Nebula LIB is distributed as source. Add the `src/` directory to ReplicatedStorage under a `Packages/Nebula` ModuleScript folder, or use Rojo to map the repository's `src/` directory.

No external package manager or HTTP request is required at runtime.
